Требования к хостингу:
CPU: 2-3 ядра.
RAM: 2-3 GB.
Хранилище: 5-10 GB (с возможностью увеличения).
Трафик: 1-2 GB в месяц.
Поддержка Docker: Да.
Резервное копирование: нет
Бюджет: $10-20 в месяц.

340 - www.netangels.ru/ + SSL
650 - https://clo.ru/ - без ддос защиты
500 - /sweb.ru/ + докер -SSL-ddos +domen .online
----------
q90.online
coffeetap.online
coffeegeek.online
----------

Чтобы ваше приложение, запущенное на домашнем компьютере, было доступно из интернета, нужно выполнить несколько шагов.

---

### 1. **Настройте порты в `docker-compose.yml`**
Убедитесь, что порты ваших сервисов проброшены на хост. Например:

```yaml
services:
  backend:
    ports:
      - "8000:8000"  # Порт backend

  frontend:
    ports:
      - "5000:5000"  # Порт frontend
```

---

### 2. **Настройте роутер для проброса портов (Port Forwarding)**
Чтобы ваш домашний сервер был доступен из интернета, нужно настроить проброс портов на роутере.

#### Шаги:
1. **Узнайте локальный IP-адрес вашего компьютера**:
   - На Linux/macOS: выполните `ifconfig` или `ip a`.
   - На Windows: выполните `ipconfig`.
   - Найдите IP-адрес в сети (например, `192.168.1.100`).

2. **Зайдите в настройки роутера**:
   - Обычно это делается через браузер, введя IP-адрес роутера (например, `192.168.1.1`).
   - Логин и пароль по умолчанию часто указаны на роутере.

3. **Настройте Port Forwarding**:
   - Найдите раздел "Port Forwarding" или "Проброс портов".
   - Добавьте правила для портов, которые вы используете (например, `8000` для backend и `5000` для frontend).
   - Укажите локальный IP-адрес вашего компьютера.

   Пример настроек:
   - **Внешний порт**: `8000` → **Внутренний порт**: `8000` → **Локальный IP**: `192.168.1.100`.
   - **Внешний порт**: `5000` → **Внутренний порт**: `5000` → **Локальный IP**: `192.168.1.100`.

4. **Сохраните настройки и перезагрузите роутер**.

---

### 3. **Узнайте внешний IP-адрес**
1. **Найдите ваш внешний IP-адрес**:
   - Перейдите на сайт [https://whatismyipaddress.com/](https://whatismyipaddress.com/).
   - Запишите ваш внешний IP-адрес (например, `123.45.67.89`).

2. **Проверьте доступность портов**:
   - Используйте сайт [https://canyouseeme.org/](https://canyouseeme.org/).
   - Введите порт (например, `8000`) и проверьте, открыт ли он.

---

### 4. **Настройте динамический DNS (опционально)**
Если у вас динамический IP-адрес (он может меняться), используйте сервис динамического DNS (DDNS), чтобы получить постоянное доменное имя.

#### Популярные сервисы DDNS:
- [No-IP](https://www.noip.com/)
- [DuckDNS](https://www.duckdns.org/)

#### Шаги:
1. Зарегистрируйтесь на выбранном сервисе.
2. Настройте DDNS на роутере или установите клиент на компьютер.
3. Используйте доменное имя (например, `myapp.ddns.net`) вместо IP-адреса.

---

### 5. **Настройте фаервол**
Убедитесь, что на вашем компьютере разрешены входящие соединения на нужные порты.

#### Для Linux (Ubuntu/Debian):
```bash
sudo ufw allow 8000/tcp
sudo ufw allow 5000/tcp
sudo ufw reload
```

---

### 6. **Запустите приложение**
1. Запустите контейнеры:
   ```bash
   docker-compose up --build
   ```

2. Проверьте доступность:
   - Backend: `http://<ваш_IP>:8000` или `http://<доменное_имя>:8000`.
   - Frontend: `http://<ваш_IP>:5000` или `http://<доменное_имя>:5000`.

---

### 7. **Используйте облачный прокси (опционально)**
Если настройка роутера сложна или у вас нет доступа к нему, можно использовать облачные сервисы для проброса портов.

#### Популярные сервисы:
- **Ngrok** (https://ngrok.com/):
  - Устанавливается на ваш компьютер.
  - Создает публичный URL для локального сервера.
  - Пример использования:
    ```bash
    ngrok http 8000
    ```
  - После запуска вы получите URL вида `https://<random>.ngrok.io`.

- **Cloudflare Tunnel** (https://www.cloudflare.com/products/tunnel/):
  - Бесплатный и безопасный способ проброса портов.
  - Устанавливается через Docker или как отдельное приложение.

---

### 8. **Рекомендации по безопасности**
- **Используйте HTTPS**: Если вы хотите защитить данные, настройте HTTPS с помощью Let's Encrypt или используйте облачный прокси (например, Ngrok или Cloudflare).
- **Ограничьте доступ**: Настройте фаервол или используйте аутентификацию, чтобы ограничить доступ к вашему приложению.
- **Обновляйте ПО**: Убедитесь, что Docker, контейнеры и роутер используют последние версии ПО.

---

### Итог
1. Настройте проброс портов на роутере.
2. Узнайте внешний IP-адрес или настройте DDNS.
3. Запустите приложение и проверьте доступность из интернета.

Теперь ваше приложение будет доступно из интернета! Если возникнут вопросы, пишите! 😊
